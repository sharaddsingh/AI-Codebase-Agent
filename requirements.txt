# Backend runtime dependencies (Python 3.10+)
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7,<3.0
pydantic-settings>=2.3
python-dotenv>=1.0
sse-starlette>=2.1
httpx>=0.27

# Model provider SDK (Claude/Anthropic; the adapter is provider-agnostic)
anthropic>=0.39,<2.0

# Official Model Context Protocol SDK (do NOT hand-roll the protocol)
mcp>=1.2

# Ignore-rule matching (.gitignore-style patterns)
pathspec>=0.12
